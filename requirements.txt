# --- Core app ---
streamlit>=1.32,<2
python-dotenv>=1.0,<2
requests>=2.31,<3

# --- LLM / RAG stack (only keep what you use) ---
openai>=1.0,<2
langchain>=0.2,<0.4
langchain-core>=0.2,<0.4
langchain-community>=0.2,<0.4
langchain-openai>=0.1,<0.4
langsmith>=0.1,<0.3

# --- Vector DB / embeddings ---
chromadb>=0.5,<2
sentence-transformers>=2.6,<4

# --- Data / utils ---
numpy>=1.26,<3
pandas>=2.0,<3
pyyaml>=6,<7
tqdm>=4.66,<5
orjson>=3.9,<4

# --- Optional: audio / speech (REMOVE any section you don't use) ---
gTTS>=2.5,<3
SpeechRecognition>=3.10,<4
streamlit-mic-recorder==0.0.8

# If you use faster-whisper transcription (HEAVY; may increase build time)
faster-whisper==1.2.1
ctranslate2>=4.0,<5

# Needed by some websocket/async libraries (safe)
aiohttp>=3.9,<4
aiodns>=3.1,<4
pycares>=4.4,<5
